import React, { Fragment } from 'react';

const Spinner = () => (
    <Fragment>
        <i className="fa fa-spinner fa-spin " style={{ width: '200px', margin: 'auto', display: 'block' }} alt='Loading'></i>
    </Fragment>
);

export default Spinner;